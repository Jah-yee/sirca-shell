.pragma library
// Written by glass-mode (glass-desktop/tools/mode.sh) on every theme / mode switch, so the lock screen wears the same colour
// theme, light or dark look and wallpaper as the desktop. These are the defaults when glass-mode has never run.
var accent = "#4c6ed6";
var dark = true;
var wallpaper = "";          // "" = the bundled assets/background.jpg
